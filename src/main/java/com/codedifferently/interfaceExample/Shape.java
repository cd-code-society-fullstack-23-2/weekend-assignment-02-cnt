// Declare that this code belongs to a package named "com.codedifferently.interfaceExample"
package com.codedifferently.interfaceExample;

// Declare an interface named "Shape"
public interface Shape {
    // Declare an abstract method named "area" inside the interface
    double area();

    // Note: In interfaces, methods are declared but not implemented (no code inside the method).
    // Classes that implement this interface must provide their own implementation of the "area" method.
}

